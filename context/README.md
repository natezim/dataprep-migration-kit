# Placeholder — do not delete

Some Gemini / starter-kit setups auto-load a `./context/` directory and error
("directory context does not exist") if it is missing. This empty folder satisfies that.

Migration recipe inputs do NOT live here — they live in `flows/<plan>/<flow>/recipe/`.
